import { tokens } from './theme/tokens';
import {
  AppShell,
  BottomTabBar,
  EvidenceTimeline,
  FieldCard,
  PrimaryButton,
  RecordListItem,
  SectionHeader,
  StatusChip,
  TopBar,
  TrackerCard,
} from './ui';

export const designContract = {
  tokens,
  primitives: {
    AppShell,
    BottomTabBar,
    EvidenceTimeline,
    FieldCard,
    PrimaryButton,
    RecordListItem,
    SectionHeader,
    StatusChip,
    TopBar,
    TrackerCard,
  },
};
