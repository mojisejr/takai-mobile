import { useEffect, useState } from 'react';
import { Pressable, StyleSheet, Text, TextInput, View } from 'react-native';
import { DatePickerField, FieldCard, FormFeedback, IconDisc, MultiSearchPickerSheet, PickerField, PrimaryButton, SearchPickerSheet, SectionHeader, StickySaveBar } from '../../ui';
import { tokens } from '../../theme/tokens';
import type { LaborV2Chemical, LaborV2Plot, LaborWorker } from './types';
import type { Ready } from './LaborMvpApp';
import { selectLaborWebProofScenario } from './webProofVisual';

type WageMethod = 'daily' | 'hourly' | 'contract';
type PlotTargetDraft = { plotId: string; treeLabels: string[] };
type ChemicalUseDraft = { chemicalId?: string; quickAdd?: { commonName: string; referenceAmount: string; referenceUnit: string; referenceWaterLitres: string; addedOn: string }; markEmpty: boolean };
type ChemicalMixDraft = { waterLitres: string; uses: ChemicalUseDraft[] };
type Job = { id: string; title: string; hours: string; minutes: string; plotTargets: PlotTargetDraft[]; chemicalMix?: ChemicalMixDraft };
type WorkGroup = { id: string; method: WageMethod; personId: string | null; memberIds: string[]; jobs: Job[]; dayPart: 'full' | 'half'; rateBaht: string; noWage: boolean; contractTitle: string };
type Props = { ready: Ready; refresh: () => Promise<void>; notify: (text: string) => void; onPayment: () => void; onManagePlots: () => void };

const createJob = (): Job => ({ id: `job-${Date.now()}-${Math.random().toString(36).slice(2)}`, title: '', hours: '', minutes: '', plotTargets: [] });
const durationMinutes = (job: Job) => Number(job.hours || 0) * 60 + Number(job.minutes || 0);
const createGroup = (method: WageMethod = 'daily'): WorkGroup => ({ id: `group-${Date.now()}-${Math.random().toString(36).slice(2)}`, method, personId: null, memberIds: [], jobs: [createJob()], dayPart: 'full', rateBaht: '', noWage: false, contractTitle: '' });
const nameFor = (workers: LaborWorker[], id: string | null) => workers.find((worker) => worker.id === id)?.displayName ?? '';
const bahtToSatang = (value: string) => Math.round(Number(value) * 100);
const methodLabel = (method: WageMethod) => method === 'daily' ? 'ค่าแรงรายวัน' : method === 'hourly' ? 'ค่าแรงรายชั่วโมง' : 'งานเหมา';
const dayPartLabel = (dayPart: WorkGroup['dayPart']) => dayPart === 'full' ? 'เต็มวัน' : 'ครึ่งวัน';
const chemicalMixPlotGuidance = 'งานที่ใส่ยาเลือกได้ 1 แปลงเท่านั้น หากต้องใช้คนละแปลงหรือคนละสูตร ให้เพิ่มงานใหม่';

/** Capture owns optional work context only; no selected plot remains ordinary general work. */
export function LaborRecordGroupEditor({ ready, refresh, notify, onPayment, onManagePlots }: Props) {
  const scenario = selectLaborWebProofScenario();
  const su = 'labor-v2-preview-su'; const phuang = 'labor-v2-preview-phuang';
  const [workDate, setWorkDate] = useState(scenario ? '2026-08-21' : ready.date);
  const [groups, setGroups] = useState<WorkGroup[]>(() => {
    if (scenario === 'daily-three-task') return [{ ...createGroup('daily'), id: 'proof-daily', personId: su, rateBaht: '350', jobs: [{ ...createJob(), id: 'proof-cut', title: 'ตัดหญ้า' }, { ...createJob(), id: 'proof-fertilize', title: 'ใส่ปุ๋ย' }, { ...createJob(), id: 'proof-spray', title: 'พ่นยา' }] }];
    if (scenario === 'open-contract') return [{ ...createGroup('contract'), id: 'proof-contract', memberIds: [su, phuang], contractTitle: 'กรอกถุงเพาะชำ', jobs: [{ ...createJob(), id: 'proof-bags', title: 'กรอกถุงเพาะชำ' }] }];
    if (scenario === 'plot-multi-target' || scenario === 'plot-quick-add') return [{ ...createGroup('daily'), id: 'proof-plots', personId: su, rateBaht: '350', jobs: [{ ...createJob(), id: 'proof-plot-task', title: 'พ่นยารอบเช้า', plotTargets: [{ plotId: 'labor-v2-preview-plot-north', treeLabels: ['A-014'] }, { plotId: 'labor-v2-preview-plot-pond', treeLabels: ['P-003'] }] }] }];
    return [createGroup()];
  });
  const [picker, setPicker] = useState<{ groupId: string; kind: 'person' | 'team' } | null>(null);
  const [plotPicker, setPlotPicker] = useState<{ groupId: string; jobId: string } | null>(() => scenario === 'plot-quick-add' ? { groupId: 'proof-plots', jobId: 'proof-plot-task' } : null);
  const [chemicalPicker, setChemicalPicker] = useState<{ groupId: string; jobId: string } | null>(null);
  const [methodPickerGroup, setMethodPickerGroup] = useState<string | null>(null);
  const [dayPartPickerGroup, setDayPartPickerGroup] = useState<string | null>(null);
  const [query, setQuery] = useState('');
  const [plots, setPlots] = useState<LaborV2Plot[]>([]);
  const [chemicals, setChemicals] = useState<LaborV2Chemical[]>([]);
  const [quickPlotName, setQuickPlotName] = useState(scenario === 'plot-quick-add' ? 'แปลงใหม่ทดสอบ' : '');
  const [quickChemical, setQuickChemical] = useState({ commonName: '', referenceAmount: '', referenceUnit: 'g', referenceWaterLitres: '200' });
  const [saving, setSaving] = useState(false);
  const workers = ready.workers.filter((worker) => !worker.archivedAt);
  const options = workers.map((worker) => ({ id: worker.id, label: worker.displayName, meta: worker.specialty }));
  const plotOptions = plots.map((plot) => ({ id: plot.id, label: plot.name, meta: plot.cropLabel || 'ยังไม่ได้ระบุพืชที่ปลูก' }));
  const chemicalOptions = chemicals.filter((chemical) => chemical.status === 'available').map((chemical) => ({ id: chemical.id, label: chemical.commonName, meta: `${chemical.referenceAmount} ${chemical.referenceUnit} / ${chemical.referenceWaterLitres} ลิตร` }));
  const loadPlots = async () => { try { setPlots(await ready.adapter.plots.list()); } catch { notify('เปิดรายการแปลงไม่สำเร็จ'); } };
  const loadChemicals = async () => { try { setChemicals(await ready.adapter.chemicals.list()); } catch { notify('เปิดคลังยาไม่สำเร็จ'); } };
  useEffect(() => { void loadPlots(); void loadChemicals(); }, [ready.adapter]);
  const update = (groupId: string, patch: Partial<WorkGroup>) => setGroups((items) => items.map((group) => group.id === groupId ? { ...group, ...patch } : group));
  const updateJob = (groupId: string, jobId: string, patch: Partial<Job>) => setGroups((items) => items.map((group) => group.id === groupId ? { ...group, jobs: group.jobs.map((job) => job.id === jobId ? { ...job, ...patch } : job) } : group));
  const addJob = (groupId: string) => update(groupId, { jobs: [...(groups.find((group) => group.id === groupId)?.jobs ?? []), createJob()] });
  const removeJob = (groupId: string, jobId: string) => update(groupId, { jobs: (groups.find((group) => group.id === groupId)?.jobs ?? []).length === 1 ? groups.find((group) => group.id === groupId)!.jobs : groups.find((group) => group.id === groupId)!.jobs.filter((job) => job.id !== jobId) });
  const chooseMethod = (groupId: string, method: WageMethod) => update(groupId, { method, ...(method === 'contract' ? { personId: null } : { memberIds: [] }) });
  const jobForPicker = plotPicker ? groups.find((group) => group.id === plotPicker.groupId)?.jobs.find((job) => job.id === plotPicker.jobId) : undefined;
  const jobForChemicalPicker = chemicalPicker ? groups.find((group) => group.id === chemicalPicker.groupId)?.jobs.find((job) => job.id === chemicalPicker.jobId) : undefined;
  const togglePlot = (plotId: string) => { if (!plotPicker || !jobForPicker) return; const targets = jobForPicker.plotTargets; if (targets.some((target) => target.plotId === plotId)) return updateJob(plotPicker.groupId, plotPicker.jobId, { plotTargets: targets.filter((target) => target.plotId !== plotId) }); if (jobForPicker.chemicalMix && targets.length >= 1) return notify(chemicalMixPlotGuidance); updateJob(plotPicker.groupId, plotPicker.jobId, { plotTargets: [...targets, { plotId, treeLabels: [] }] }); };
  const addQuickPlot = async () => { if (!quickPlotName.trim() || !plotPicker) return notify('กรอกชื่อแปลงก่อนเพิ่ม'); if (jobForPicker?.chemicalMix && jobForPicker.plotTargets.length >= 1) return notify(chemicalMixPlotGuidance); try { const plotId = await ready.adapter.plots.create({ name: quickPlotName.trim() }); await loadPlots(); updateJob(plotPicker.groupId, plotPicker.jobId, { plotTargets: [...(jobForPicker?.plotTargets ?? []), { plotId, treeLabels: [] }] }); setQuickPlotName(''); notify('เพิ่มแปลงและเลือกให้กับงานนี้แล้ว'); } catch (error) { notify(error instanceof Error ? error.message : 'เพิ่มแปลงไม่สำเร็จ'); } };
  const setTrees = (groupId: string, jobId: string, plotId: string, treeLabels: string[]) => { const job = groups.find((item) => item.id === groupId)?.jobs.find((item) => item.id === jobId); if (!job) return; updateJob(groupId, jobId, { plotTargets: job.plotTargets.map((target) => target.plotId === plotId ? { ...target, treeLabels } : target) }); };
  const updateChemicalMix = (groupId: string, jobId: string, patch: Partial<ChemicalMixDraft>) => { const job = groups.find((group) => group.id === groupId)?.jobs.find((item) => item.id === jobId); updateJob(groupId, jobId, { chemicalMix: { waterLitres: job?.chemicalMix?.waterLitres ?? '', uses: job?.chemicalMix?.uses ?? [], ...patch } }); };
  const setChemicalUses = (groupId: string, jobId: string, uses: ChemicalUseDraft[]) => uses.length ? updateChemicalMix(groupId, jobId, { uses }) : updateJob(groupId, jobId, { chemicalMix: undefined });
  const toggleChemical = (chemicalId: string) => { if (!chemicalPicker || !jobForChemicalPicker) return; const uses = jobForChemicalPicker.chemicalMix?.uses ?? []; if (uses.some((use) => use.chemicalId === chemicalId)) return setChemicalUses(chemicalPicker.groupId, chemicalPicker.jobId, uses.filter((use) => use.chemicalId !== chemicalId)); if (jobForChemicalPicker.plotTargets.length > 1) return notify(chemicalMixPlotGuidance); setChemicalUses(chemicalPicker.groupId, chemicalPicker.jobId, [...uses, { chemicalId, markEmpty: false }]); };
  const addQuickChemical = () => { if (!chemicalPicker || !jobForChemicalPicker) return; if (jobForChemicalPicker.plotTargets.length > 1) return notify(chemicalMixPlotGuidance); const commonName = quickChemical.commonName.trim(); const referenceAmount = Number(quickChemical.referenceAmount); const referenceUnit = quickChemical.referenceUnit.trim(); const referenceWaterLitres = Number(quickChemical.referenceWaterLitres); if (!commonName || !Number.isFinite(referenceAmount) || referenceAmount <= 0 || !referenceUnit || !Number.isFinite(referenceWaterLitres) || referenceWaterLitres <= 0) return notify('กรอกชื่อยา อัตราใช้ หน่วย และน้ำอ้างอิงให้ครบ'); const uses = jobForChemicalPicker.chemicalMix?.uses ?? []; updateChemicalMix(chemicalPicker.groupId, chemicalPicker.jobId, { uses: [...uses, { quickAdd: { commonName, referenceAmount: quickChemical.referenceAmount, referenceUnit, referenceWaterLitres: quickChemical.referenceWaterLitres, addedOn: workDate }, markEmpty: false }] }); setQuickChemical({ commonName: '', referenceAmount: '', referenceUnit: 'g', referenceWaterLitres: '200' }); };

  const save = async () => {
    const invalid = groups.find((group) => !group.jobs.some((job) => job.title.trim()) || (group.method === 'contract' ? !group.memberIds.length || !group.contractTitle.trim() : !group.personId));
    if (invalid) return notify('เลือกคนหรือชุดที่มาทำงาน และกรอกงานที่ทำวันนี้ให้ครบ');
    const dailyGroups = groups.filter((group) => group.method === 'daily');
    if (dailyGroups.some((group, index) => dailyGroups.slice(index + 1).some((next) => next.personId === group.personId))) return notify('คนเดียวกันในวันเดียว บันทึกค่าแรงรายวันเป็นชุดเดียว');
    for (const group of groups) {
      if (group.method === 'daily' && !group.noWage && (!Number.isSafeInteger(bahtToSatang(group.rateBaht)) || bahtToSatang(group.rateBaht) <= 0)) return notify('กรอกค่าแรงรายวัน หรือเลือก “ไม่มีค่าแรง” ให้ชัดเจน');
      if (group.method === 'hourly' && (!Number.isSafeInteger(bahtToSatang(group.rateBaht)) || bahtToSatang(group.rateBaht) <= 0 || group.jobs.some((job) => !Number.isInteger(Number(job.hours || 0)) || Number(job.hours || 0) < 0 || !Number.isInteger(Number(job.minutes || 0)) || Number(job.minutes || 0) < 0 || Number(job.minutes || 0) >= 60 || durationMinutes(job) <= 0))) return notify('กรอกค่าแรงรายชั่วโมง และเวลาเป็นชั่วโมงกับนาทีของแต่ละงานให้ครบ');
      for (const job of group.jobs) { for (const target of job.plotTargets) { const trees = target.treeLabels.map((label) => label.trim()).filter(Boolean); if (new Set(trees).size !== trees.length) return notify('ชื่อต้นที่เกี่ยวข้องในแปลงเดียวกันซ้ำกัน'); } if (job.chemicalMix && job.plotTargets.length > 1) return notify(chemicalMixPlotGuidance); if (job.chemicalMix && (!job.chemicalMix.uses.length || !Number.isFinite(Number(job.chemicalMix.waterLitres)) || Number(job.chemicalMix.waterLitres) <= 0)) return notify('เลือกยาอย่างน้อย 1 รายการ และกรอกปริมาณน้ำที่ใช้'); }
    }
    const taskFacts = groups.flatMap((group) => group.jobs.filter((job) => job.title.trim()).map((job) => ({ id: `${group.id}:${job.id}`, title: job.title.trim(), assigneePersonIds: group.method === 'contract' ? group.memberIds : [group.personId!], plotTargets: job.plotTargets.map((target) => ({ plotId: target.plotId, treeLabels: target.treeLabels.map((label) => label.trim()).filter(Boolean) })), ...(job.chemicalMix ? { chemicalMix: { waterLitres: Number(job.chemicalMix.waterLitres), uses: job.chemicalMix.uses.map((use) => ({ ...(use.chemicalId ? { chemicalId: use.chemicalId } : { quickAdd: { commonName: use.quickAdd!.commonName, referenceAmount: Number(use.quickAdd!.referenceAmount), referenceUnit: use.quickAdd!.referenceUnit, referenceWaterLitres: Number(use.quickAdd!.referenceWaterLitres), addedOn: use.quickAdd!.addedOn } }), markEmpty: use.markEmpty, ...(use.markEmpty ? { emptyReason: 'ระบุว่าใช้หมดแล้วตอนบันทึกงาน' } : {}) })) } } : {}) })));
    const daily = groups.filter((group) => group.method === 'daily' && !group.noWage).map((group) => ({ personId: group.personId!, rateSatang: bahtToSatang(group.rateBaht), quantityMilli: group.dayPart === 'full' ? 1000 as const : 500 as const, taskIds: group.jobs.filter((job) => job.title.trim()).map((job) => `${group.id}:${job.id}`) }));
    const hourly = groups.filter((group) => group.method === 'hourly').flatMap((group) => group.jobs.filter((job) => job.title.trim()).map((job) => ({ id: `${group.id}:${job.id}`, taskId: `${group.id}:${job.id}`, personId: group.personId!, rateSatang: bahtToSatang(group.rateBaht), shiftKey: group.id, durationMinutes: durationMinutes(job) })));
    setSaving(true);
    try { await ready.adapter.commands.recordDay({ workDate, tasks: taskFacts, daily, hourly }); for (const group of groups.filter((item) => item.method === 'contract')) await ready.adapter.commands.startContract({ title: group.contractTitle.trim(), startsOn: workDate, memberPersonIds: group.memberIds, taskIds: group.jobs.filter((job) => job.title.trim()).map((job) => `${group.id}:${job.id}`) }); await refresh(); setGroups([createGroup()]); notify('บันทึกงานและค่าแรงแล้ว'); } catch (error) { notify(error instanceof Error ? error.message : 'บันทึกงานไม่สำเร็จ'); } finally { setSaving(false); }
  };

  return <View style={styles.screen}>
    <DatePickerField label="วันที่ทำงาน" value={workDate} onChange={setWorkDate} />
    <FieldCard variant="summary" style={styles.introCard}><IconDisc icon="record" size={38} tone="gold" /><View style={styles.introText}><Text style={styles.introTitle}>บันทึกงานให้จบในครั้งเดียว</Text><Text style={styles.intro}>เลือกคนหรือทีม เติมงาน และระบุแปลงเมื่อเกี่ยวข้อง</Text></View></FieldCard>
    {groups.map((group, index) => <FieldCard key={group.id} style={styles.groupCard} variant="raised">
      <View style={styles.header}><View style={styles.groupTitle}><IconDisc icon={group.method === 'contract' ? 'people' : group.method === 'hourly' ? 'work' : 'today'} size={34} /><Text style={styles.title}>ชุดงานที่ {index + 1}</Text></View>{groups.length > 1 ? <Pressable onPress={() => setGroups((items) => items.filter((item) => item.id !== group.id))}><Text style={styles.remove}>ลบชุดนี้</Text></Pressable> : null}</View>
      {group.method === 'contract' ? <PickerField label="ชุดคนที่มาทำงาน" value={group.memberIds.map((id) => nameFor(workers, id)).filter(Boolean).join(', ')} placeholder="เลือกคนทำงานอย่างน้อย 1 คน" onPress={() => setPicker({ groupId: group.id, kind: 'team' })} /> : <PickerField label="คนที่มาทำงาน" value={nameFor(workers, group.personId)} placeholder="เลือกคนทำงาน" onPress={() => setPicker({ groupId: group.id, kind: 'person' })} />}
      <SectionHeader title="งานที่ทำวันนี้" />
      {group.jobs.map((job, jobIndex) => <View key={job.id} style={styles.jobBlock}><View style={group.method === 'hourly' ? styles.hourlyJob : styles.jobRow}><TextInput accessibilityLabel={`งานที่ทำวันนี้ ${jobIndex + 1}`} onChangeText={(title) => updateJob(group.id, job.id, { title })} placeholder={`งานที่ ${jobIndex + 1}`} style={styles.input} value={job.title} />{group.method === 'hourly' ? <View style={styles.duration}><TextInput accessibilityLabel={`ชั่วโมงงาน ${jobIndex + 1}`} keyboardType="number-pad" onChangeText={(hours) => updateJob(group.id, job.id, { hours })} placeholder="ชั่วโมง" style={[styles.input, styles.durationInput]} value={job.hours} /><TextInput accessibilityLabel={`นาทีงาน ${jobIndex + 1}`} keyboardType="number-pad" onChangeText={(minutes) => updateJob(group.id, job.id, { minutes })} placeholder="นาที" style={[styles.input, styles.durationInput]} value={job.minutes} /></View> : null}</View>
        <PickerField label={job.chemicalMix ? 'พื้นที่ที่ทำงาน (ชุดยานี้เลือกได้ 1 แปลง)' : 'พื้นที่ที่ทำงาน (ไม่บังคับ)'} value={job.plotTargets.length ? `${job.plotTargets.length} แปลง` : ''} placeholder="งานทั่วไป ไม่ระบุแปลง" onPress={() => { setQuery(''); setPlotPicker({ groupId: group.id, jobId: job.id }); }} />
        {job.chemicalMix ? <Text style={styles.caption}>{chemicalMixPlotGuidance}</Text> : null}
        {job.plotTargets.map((target) => <PlotTargetRows key={target.plotId} plot={plots.find((plot) => plot.id === target.plotId)} target={target} onRemove={() => updateJob(group.id, job.id, { plotTargets: job.plotTargets.filter((item) => item.plotId !== target.plotId) })} onTrees={(treeLabels) => setTrees(group.id, job.id, target.plotId, treeLabels)} />)}
        <PickerField label="ยา / เคมีที่ใช้ (ไม่บังคับ)" value={job.chemicalMix?.uses.length ? `${job.chemicalMix.uses.length} รายการ` : ''} placeholder="ไม่มีรายการยาในงานนี้" onPress={() => { setQuery(''); setChemicalPicker({ groupId: group.id, jobId: job.id }); }} />
        {job.chemicalMix ? <FieldCard style={styles.targetCard}><Text style={styles.treeLabel}>น้ำที่ใช้ร่วมกันทั้งชุดยา</Text><TextInput accessibilityLabel={`ปริมาณน้ำงาน ${jobIndex + 1}`} keyboardType="decimal-pad" onChangeText={(waterLitres) => updateChemicalMix(group.id, job.id, { waterLitres })} placeholder="เช่น 200 ลิตร" style={styles.input} value={job.chemicalMix.waterLitres} /><Text style={styles.caption}>ระบบคำนวณอัตราใช้ของยาแต่ละตัวจากน้ำก้อนนี้ให้เอง</Text>{job.chemicalMix.uses.map((use, useIndex) => <ChemicalUseRows key={`${use.chemicalId ?? use.quickAdd?.commonName}-${useIndex}`} waterLitres={Number(job.chemicalMix!.waterLitres)} use={use} chemical={use.chemicalId ? chemicals.find((chemical) => chemical.id === use.chemicalId) : undefined} onRemove={() => setChemicalUses(group.id, job.id, job.chemicalMix!.uses.filter((_, position) => position !== useIndex))} onToggleEmpty={() => updateChemicalMix(group.id, job.id, { uses: job.chemicalMix!.uses.map((item, position) => position === useIndex ? { ...item, markEmpty: !item.markEmpty } : item) })} />)}</FieldCard> : null}
        <Pressable accessibilityLabel={`ลบงานที่ ${jobIndex + 1}`} onPress={() => removeJob(group.id, job.id)} style={styles.removeAction}><Text style={styles.remove}>ลบงานนี้</Text></Pressable>
      </View>)}
      <PrimaryButton label="+ เพิ่มงาน" onPress={() => addJob(group.id)} variant="secondary" />
      <SectionHeader title="วิธีคิดค่าแรง" />
      <PickerField label="คิดค่าแรงแบบ" value={methodLabel(group.method)} placeholder="เลือกวิธีคิดค่าแรง" onPress={() => setMethodPickerGroup(group.id)} />
      {group.method === 'daily' ? <View style={styles.detail}><PickerField label="ช่วงเวลาทำงาน" value={dayPartLabel(group.dayPart)} placeholder="เลือกเต็มวันหรือครึ่งวัน" onPress={() => setDayPartPickerGroup(group.id)} /><Pressable accessibilityRole="checkbox" accessibilityState={{ checked: group.noWage }} onPress={() => update(group.id, { noWage: !group.noWage, ...(group.noWage ? {} : { rateBaht: '' }) })} style={[styles.noWage, group.noWage && styles.noWageSelected]}><Text style={group.noWage ? styles.methodTextSelected : styles.methodText}>{group.noWage ? '✓ ไม่มีค่าแรงสำหรับชุดงานนี้' : 'ไม่มีค่าแรงสำหรับชุดงานนี้'}</Text></Pressable>{!group.noWage ? <TextInput keyboardType="number-pad" onChangeText={(rateBaht) => update(group.id, { rateBaht })} placeholder="ค่าแรงต่อวัน (บาท)" style={styles.input} value={group.rateBaht} /> : <Text style={styles.caption}>บันทึกเฉพาะงานที่ทำ โดยไม่สร้างค่าแรงค้างจ่าย</Text>}<Text style={styles.caption}>งานทั้งหมดของคนนี้ในวันนี้รวมเป็นค่าแรงก้อนเดียว</Text></View> : null}
      {group.method === 'hourly' ? <View style={styles.detail}><TextInput keyboardType="number-pad" onChangeText={(rateBaht) => update(group.id, { rateBaht })} placeholder="ค่าแรงต่อชั่วโมง (บาท)" style={styles.input} value={group.rateBaht} /><Text style={styles.caption}>ใส่เวลาของแต่ละงานเป็นชั่วโมงกับนาที เพื่อรวมเป็นกะเดียวอย่างถูกต้อง</Text></View> : null}
      {group.method === 'contract' ? <View style={styles.detail}><TextInput onChangeText={(contractTitle) => update(group.id, { contractTitle })} placeholder="ชื่องานเหมา" style={styles.input} value={group.contractTitle} /><Text style={styles.caption}>เปิดงานไว้ก่อนได้ ยังไม่ต้องใส่จำนวนหรือราคา และยังไม่มีค่าแรงค้างจ่าย</Text></View> : null}
    </FieldCard>)}
    <PrimaryButton label="+ เพิ่มชุดงาน" onPress={() => setGroups((items) => [...items, createGroup()])} variant="secondary" />
    <FormFeedback kind="notice">บันทึกงานก่อน การจ่ายเงินอยู่ในหน้า “จ่ายเงิน” แยกต่างหาก</FormFeedback>
    <StickySaveBar disabled={saving} label={saving ? 'กำลังบันทึก…' : 'บันทึกงาน'} onPress={save} />
    <PrimaryButton label="ไปที่จ่ายเงิน" onPress={onPayment} variant="secondary" />
    <MultiSearchPickerSheet visible={picker?.kind === 'team'} title="เลือกคนในชุดงานเหมา" query={query} setQuery={setQuery} options={options} selectedIds={picker?.kind === 'team' ? groups.find((group) => group.id === picker.groupId)?.memberIds ?? [] : []} onToggle={(id) => { if (!picker || picker.kind !== 'team') return; const selected = groups.find((group) => group.id === picker.groupId)?.memberIds ?? []; update(picker.groupId, { memberIds: selected.includes(id) ? selected.filter((item) => item !== id) : [...selected, id] }); }} onClose={() => setPicker(null)} emptyLabel="ยังไม่มีคนทำงาน" />
    <MultiSearchPickerSheet visible={picker?.kind === 'person'} title="เลือกคนทำงาน" query={query} setQuery={setQuery} options={options} selectedIds={picker?.kind === 'person' && groups.find((group) => group.id === picker.groupId)?.personId ? [groups.find((group) => group.id === picker.groupId)!.personId!] : []} onToggle={(id) => { if (!picker || picker.kind !== 'person') return; update(picker.groupId, { personId: id }); setPicker(null); }} onClose={() => setPicker(null)} emptyLabel="ยังไม่มีคนทำงาน" />
    <MultiSearchPickerSheet visible={Boolean(plotPicker)} title="เลือกพื้นที่ที่ทำงาน" query={query} setQuery={setQuery} options={plotOptions} selectedIds={jobForPicker?.plotTargets.map((target) => target.plotId) ?? []} selectionLabel={jobForPicker?.chemicalMix ? 'แปลง (เลือกได้ 1)' : 'แปลง'} onToggle={togglePlot} onClose={() => setPlotPicker(null)} emptyLabel="ยังไม่มีแปลงที่ใช้งาน" quickAdd={<><Text style={styles.quickAddTitle}>เพิ่มแปลงเล็ก ๆ แล้วเลือกทันที</Text>{jobForPicker?.chemicalMix ? <Text style={styles.caption}>{chemicalMixPlotGuidance}</Text> : null}<TextInput accessibilityLabel="ชื่อแปลงใหม่" style={styles.input} value={quickPlotName} onChangeText={setQuickPlotName} placeholder="ชื่อแปลง" /><PrimaryButton label="เพิ่มแปลงนี้" variant="secondary" onPress={() => void addQuickPlot()} /><Pressable onPress={onManagePlots}><Text style={styles.manageLink}>จัดการแปลงทั้งหมด</Text></Pressable></>} />
    <MultiSearchPickerSheet visible={Boolean(chemicalPicker)} title="เลือกยา / เคมีที่ใช้" query={query} setQuery={setQuery} options={chemicalOptions} selectedIds={jobForChemicalPicker?.chemicalMix?.uses.flatMap((use) => use.chemicalId ? [use.chemicalId] : []) ?? []} selectionLabel="ยา" onToggle={toggleChemical} onClose={() => setChemicalPicker(null)} emptyLabel="ยังไม่มียาที่พร้อมใช้" quickAdd={<><Text style={styles.quickAddTitle}>เพิ่มยาแล้วเลือกกับงานนี้ทันที</Text><TextInput accessibilityLabel="ชื่อสามัญยาใหม่" style={styles.input} value={quickChemical.commonName} onChangeText={(commonName) => setQuickChemical((item) => ({ ...item, commonName }))} placeholder="ชื่อสามัญ เช่น แมนโคเซบ" /><View style={styles.duration}><TextInput accessibilityLabel="อัตราใช้ยาใหม่" keyboardType="decimal-pad" style={[styles.input, styles.durationInput]} value={quickChemical.referenceAmount} onChangeText={(referenceAmount) => setQuickChemical((item) => ({ ...item, referenceAmount }))} placeholder="อัตราใช้" /><TextInput accessibilityLabel="หน่วยอัตราใช้ยาใหม่" style={[styles.input, styles.durationInput]} value={quickChemical.referenceUnit} onChangeText={(referenceUnit) => setQuickChemical((item) => ({ ...item, referenceUnit }))} placeholder="g หรือ cc" /></View><TextInput accessibilityLabel="น้ำอ้างอิงยาใหม่" keyboardType="decimal-pad" style={styles.input} value={quickChemical.referenceWaterLitres} onChangeText={(referenceWaterLitres) => setQuickChemical((item) => ({ ...item, referenceWaterLitres }))} placeholder="ต่อน้ำกี่ลิตร เช่น 200" /><PrimaryButton label="เพิ่มยานี้เข้าชุด" variant="secondary" onPress={addQuickChemical} /></>} />
    <SearchPickerSheet visible={Boolean(methodPickerGroup)} title="เลือกวิธีคิดค่าแรง" query={query} setQuery={setQuery} options={[{ id: 'daily', label: 'ค่าแรงรายวัน', meta: 'เต็มวันหรือครึ่งวัน · หลายงานรวมค่าแรงเดียว' }, { id: 'hourly', label: 'ค่าแรงรายชั่วโมง', meta: 'ระบุชั่วโมงและนาทีของแต่ละงาน' }, { id: 'contract', label: 'งานเหมา', meta: 'เปิดชุดงานก่อน ยังไม่ต้องใส่ราคา' }]} recentIds={[]} onPick={(method) => { if (methodPickerGroup) chooseMethod(methodPickerGroup, method as WageMethod); setMethodPickerGroup(null); }} onClose={() => setMethodPickerGroup(null)} emptyLabel="ไม่มีวิธีคิดค่าแรง" />
    <SearchPickerSheet visible={Boolean(dayPartPickerGroup)} title="เลือกช่วงเวลาทำงาน" query={query} setQuery={setQuery} options={[{ id: 'full', label: 'เต็มวัน', meta: 'คิดเป็น 1 วัน' }, { id: 'half', label: 'ครึ่งวัน', meta: 'คิดเป็น 0.5 วัน' }]} recentIds={[]} onPick={(dayPart) => { if (dayPartPickerGroup) update(dayPartPickerGroup, { dayPart: dayPart as WorkGroup['dayPart'] }); setDayPartPickerGroup(null); }} onClose={() => setDayPartPickerGroup(null)} emptyLabel="ไม่มีช่วงเวลางาน" />
  </View>;
}

function PlotTargetRows({ plot, target, onRemove, onTrees }: { plot?: LaborV2Plot; target: PlotTargetDraft; onRemove: () => void; onTrees: (trees: string[]) => void }) {
  return <FieldCard style={styles.targetCard}><View style={styles.targetHeader}><View style={styles.targetText}><Text style={styles.targetTitle}>{plot?.name ?? 'แปลงที่บันทึกไว้'}</Text><Text style={styles.caption}>{plot?.cropLabel || 'พื้นที่ที่เลือกสำหรับงานนี้'}</Text></View><Pressable onPress={onRemove}><Text style={styles.remove}>นำแปลงออก</Text></Pressable></View><Text style={styles.treeLabel}>ต้นที่เกี่ยวข้อง (ไม่บังคับ)</Text>{target.treeLabels.map((tree, index) => <View key={`${target.plotId}:${index}`} style={styles.treeRow}><TextInput accessibilityLabel={`ต้นที่เกี่ยวข้อง ${index + 1}`} style={styles.input} value={tree} onChangeText={(value) => onTrees(target.treeLabels.map((item, position) => position === index ? value : item))} placeholder="เช่น A-014 หรือ ต้นริมรั้ว" /><Pressable onPress={() => onTrees(target.treeLabels.filter((_, position) => position !== index))}><Text style={styles.remove}>ลบ</Text></Pressable></View>)}<Pressable onPress={() => onTrees([...target.treeLabels, ''])}><Text style={styles.manageLink}>+ ระบุต้นที่เกี่ยวข้อง</Text></Pressable></FieldCard>;
}

function ChemicalUseRows({ use, chemical, waterLitres, onRemove, onToggleEmpty }: { use: ChemicalUseDraft; chemical?: LaborV2Chemical; waterLitres: number; onRemove: () => void; onToggleEmpty: () => void }) {
  const name = chemical?.commonName ?? use.quickAdd?.commonName ?? 'ยา'; const amount = chemical?.referenceAmount ?? Number(use.quickAdd?.referenceAmount); const unit = chemical?.referenceUnit ?? use.quickAdd?.referenceUnit ?? ''; const water = chemical?.referenceWaterLitres ?? Number(use.quickAdd?.referenceWaterLitres);
  const calculated = Number.isFinite(waterLitres) && waterLitres > 0 && Number.isFinite(amount) && amount > 0 && Number.isFinite(water) && water > 0 ? amount * waterLitres / water : null;
  return <View style={styles.chemicalRow}><View style={styles.targetText}><Text style={styles.targetTitle}>{name}</Text><Text style={styles.caption}>{amount} {unit} / {water} ลิตร{calculated === null ? '' : ` · ใส่ประมาณ ${calculated} ${unit}`}</Text></View><Pressable accessibilityRole="checkbox" accessibilityState={{ checked: use.markEmpty }} onPress={onToggleEmpty} style={[styles.emptyToggle, use.markEmpty && styles.noWageSelected]}><Text style={use.markEmpty ? styles.methodTextSelected : styles.methodText}>{use.markEmpty ? '✓ ใช้หมดแล้ว' : 'ใช้หมดแล้ว'}</Text></Pressable><Pressable onPress={onRemove}><Text style={styles.remove}>นำออก</Text></Pressable></View>;
}

const styles = StyleSheet.create({
  screen: { gap: 16 }, introCard: { alignItems: 'flex-start', flexDirection: 'row', gap: 12 }, introText: { flex: 1, minWidth: 0 }, introTitle: { color: tokens.color.text.primary, fontSize: tokens.typography.h3.size, fontWeight: '800' }, groupCard: { gap: 16 }, header: { alignItems: 'center', flexDirection: 'row', justifyContent: 'space-between' }, groupTitle: { alignItems: 'center', flex: 1, flexDirection: 'row', gap: 10, minWidth: 0 }, title: { color: tokens.color.text.primary, fontSize: tokens.typography.h2.size, fontWeight: '800' }, intro: { color: tokens.color.text.muted, fontSize: tokens.typography.body.size, lineHeight: 22 }, input: { backgroundColor: tokens.color.surface.card, borderColor: tokens.color.border.soft, borderRadius: tokens.radius.row, borderWidth: 1, color: tokens.color.text.primary, flex: 1, fontSize: tokens.typography.body.size, minHeight: 48, paddingHorizontal: 14 }, jobBlock: { gap: 10 }, jobRow: { alignItems: 'center', flexDirection: 'row', gap: 8 }, hourlyJob: { gap: 8 }, duration: { flexDirection: 'row', gap: 8 }, durationInput: { flex: 1, minWidth: 0 }, removeAction: { alignSelf: 'flex-start', minHeight: 32, paddingTop: 2 }, remove: { color: tokens.color.text.muted, fontSize: tokens.typography.caption.size }, noWage: { alignItems: 'center', borderColor: tokens.color.border.soft, borderRadius: tokens.radius.row, borderWidth: 1, justifyContent: 'center', minHeight: 44, paddingHorizontal: 12 }, noWageSelected: { backgroundColor: tokens.color.primary.green, borderColor: tokens.color.primary.green }, methodText: { color: tokens.color.text.muted, fontSize: tokens.typography.caption.size, fontWeight: '700', textAlign: 'center' }, methodTextSelected: { color: tokens.color.text.inverse, fontSize: tokens.typography.caption.size, fontWeight: '800', textAlign: 'center' }, detail: { gap: 10 }, caption: { color: tokens.color.text.muted, fontSize: tokens.typography.caption.size, lineHeight: 18 }, targetCard: { gap: 8 }, targetHeader: { alignItems: 'center', flexDirection: 'row', gap: 10, justifyContent: 'space-between' }, targetText: { flex: 1, minWidth: 0 }, targetTitle: { color: tokens.color.text.primary, fontSize: tokens.typography.body.size, fontWeight: '800' }, treeLabel: { color: tokens.color.text.primary, fontSize: tokens.typography.metadata.size, fontWeight: '700' }, treeRow: { alignItems: 'center', flexDirection: 'row', gap: 8 }, chemicalRow: { alignItems: 'center', flexDirection: 'row', gap: 8 }, emptyToggle: { borderColor: tokens.color.border.soft, borderRadius: tokens.radius.row, borderWidth: 1, minHeight: 36, paddingHorizontal: 8, justifyContent: 'center' }, manageLink: { color: tokens.color.primary.green, fontSize: tokens.typography.metadata.size, fontWeight: '800' }, quickAddTitle: { color: tokens.color.text.primary, fontSize: tokens.typography.metadata.size, fontWeight: '800' },
});
